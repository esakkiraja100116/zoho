#include <stdio.h>
// This code will produce an error
int main()
{
    // int a = 26;
    // int b = 13;
    // if (a % b = 0) {
    // printf("%s", "It’s all good");
    // }

    // int i = 5;
    // printf("%d\n",(i)++);  //It throw error for ++(-i)

    // This is illegal, don’t try this
    // int x, y;
    // x = -4-4;
    // y = -4-(-4);
    // printf("x = % d y = % d", x, y);

    // int *p;
    // int z = 5;
    // p = &5; // Here is the error

    // int x = 23, y, c;
    // x >= 23 ? y = 4 : c = 4;
    // printf("%d", y);
}
